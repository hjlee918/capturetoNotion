{
  "name": "notion-capture-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run typecheck && vite build && esbuild src/background/index.ts --bundle --outfile=dist/background.js --platform=browser --format=iife --target=es2022 && esbuild src/content/index.ts --bundle --outfile=dist/content.js --platform=browser --format=iife --target=es2022 && cp manifest.json dist/manifest.json",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.327",
    "@types/react": "^19.1.2",
    "@types/react-dom": "^19.1.2",
    "@vitejs/plugin-react": "^5.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
